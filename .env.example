# Firebase Credentials
# Get this from your Firebase Console -> Project Settings -> Service Account -> Generate New Private Key
# The entire JSON content should be escaped and passed as a single line
FIREBASE_KEY={"type": "service_account", "project_id": "your_project_id", ...}

# Server Configuration
HOST=0.0.0.0
PORT=5000
DEBUG=False
